// Placeholder Terraform configuration.
// This file is intentionally minimal. Add your cloud provider resources here.

terraform {
  required_version = ">= 1.0"
}

// Example: you could provision a container registry or a managed k8s cluster.

output "info" {
  value = "Add provider blocks and resources for your target cloud"
}
